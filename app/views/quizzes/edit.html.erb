<% provide(:title, 'Edit this question') %>
<h1>Edit this question</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for("#{@quiz.id}") do |f| %>
        <%#= render 'shared/error_messages' %>

            <%= f.label :question %>
            <%= f.text_field :question, class: 'form-control' %>

            <%= f.label :answer_body %>
            <%= f.text_field :answer_body, class: 'form-control' %>

            <%= f.label :subject %>
            <%= f.text_field :subject.downcase, class: 'form-control'  %>
            <br><br>
            <%= f.submit "Update question", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>
