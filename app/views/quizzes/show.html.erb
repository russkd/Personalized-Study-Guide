<% provide(:title, 'Show the Quiz') %>

<%= "Quiz Id: #{@quiz.id}"  %>
<br>
<br>
<h3>Total Questions: <%=@quiz.questions.count%></h3>
<br>
<ol>
<% @quiz.questions.each do |q| %>

<li>
    <ul>

        <li><%= "Question: #{q.question}"%></li>

        <li><%= "Answer: #{q.question_answer}" %></li>  This needs to get the answer from the join

        <li><%= "Question Subject: #{q.subject}"  %></li>
    </ul>
<br>
</li>
<% end %>
</ol>

<%# if current_user.id == @quiz.user_id %>
  <%= link_to 'Edit', edit_quiz_path(@quiz) %><br>
  <%= link_to 'Destroy', @question, method: :delete, data: { confirm: 'Are you sure?' } %>
<%# end %>
